import styles from './page.module.scss'

const Destination = () => {
  return (
    <main
      className={`${styles.destination} text-accent ff-serif text-accent`}
    ></main>
  )
}

export default Destination
